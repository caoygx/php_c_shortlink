// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("shortlink", "for shortlink support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("shortlink", "enable shortlink support", "no");

if (PHP_SHORTLINK != "no") {
	EXTENSION("shortlink", "shortlink.c");
}

