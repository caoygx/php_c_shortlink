// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("imgurl", "for imgurl support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("imgurl", "enable imgurl support", "no");

if (PHP_IMGURL != "no") {
	EXTENSION("imgurl", "imgurl.c");
}

